namespace L1

/-- Placeholder: mirrors the `Spelling.v` module name only. No semantics
mechanized yet -- this file exists to prove the Lake build itself works. -/
def Code := Nat

theorem code_eq_nat : Code = Nat := rfl

end L1
