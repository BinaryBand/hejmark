# The same 100 columns `pyproject.toml` sets for ruff, so the two halves of the
# codebase wrap alike.
max_width = 100

# Keep short structs, calls and matches on one line rather than exploding them
# across four. The Python side reads that way and these modules are meant to be
# diffed against it.
use_small_heuristics = "Max"
