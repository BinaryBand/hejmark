[package]
name = "hejmark-engine"
version = "0.1.0"
edition = "2021"
description = "A hejmark engine: docs/protocol.md, spoken over stdio."

[dependencies]
serde_json = "1"

[profile.release]
overflow-checks = true

[lints.rust]
missing_docs = "warn"

[lints.clippy]
pedantic = { level = "warn", priority = -1 }
